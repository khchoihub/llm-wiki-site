---
tags: [GRAD_ABROAD_운영, 마스터프롬프트]
최종수정: 2026-09-07
규격버전: "4.0"
password: "0310"
---

# 문서 변환 마스터 프롬프트

다음 세션에는 원칙적으로 `GRAD G-014 변환해줘`처럼 짧게 지시한다. 에이전트는 루트 지침을 통해 이 파일을 읽어야 한다. 자동 라우팅이 되지 않을 때만 아래 전체 프롬프트를 붙여넣는다.

```text
C:\Users\USER\Desktop\llm-wiki의 GRAD/ABROAD 문서 한 개를 최종 서술형 문서로 변환한다.

대상 파일 또는 문서ID:
[경로 또는 ID]

1. 먼저 operations/GRAD_ABROAD/README.md, V3_PIPELINE.md, V4_PIPELINE.md, STATUS.md, EDITORIAL_EXAMPLE.md, FINAL_DOCUMENT_TEMPLATE.md, QA_CHECKLIST.md를 전부 읽는다. 근거 발굴은 V3을 계승하고, 출력 문체·강조·각주·완료 판정은 V4_PIPELINE.md가 우선한다.
1-1. 문서ID만 주어졌다면 rg로 frontmatter의 정확한 문서ID를 찾아 대상 파일을 확정한다.
1-2. `다음 문서`라면 STATUS.md에서 가장 앞선 미완료 항목을 선택한다. 홀수·짝수 세션이 명시되면 해당 트랙의 다음 미완료 항목만 선택하고 상대 트랙은 변경하지 않는다.
2. 외부 웹 검색을 사용하지 않는다. 기존 문서와 지정 DB만 내용 근거로 쓴다.
3. DB는 SQLite 읽기 전용 mode=ro로 연다.
4. 한 번에 지정된 문서 하나만 변환한다.
5. 파일명, 경로, 문서ID, 원본건수, password, 기존 [[링크 대상]]을 보존한다.
6. 기존 분석 섹션을 유지하지 말고 독자 질문 중심의 새 목차를 먼저 설계한다.
7. 필요한 절만 두고 개요-핵심 구조-조건-예외-논쟁-실제 변화-관련 문서 순으로 배열한다.
8. 불릿형 분석체를 백과사전식 연속 설명문으로 바꾼다.
8-1. 최종 목표는 게시물 분포를 요약하는 것이 아니라 고급 원자료의 작동 원리·실무 디테일·행위자의 판단·결과·반론을 발굴하여 설명하는 것이다.
8-2. `반복된다`, `두드러진다`, `논의된다`로 끝나는 소재 분류 문장을 핵심 서술로 사용하지 않는다. 그 자료가 실제로 무엇을 밝혀 주는지 편집자가 판단하여 쓴다.
8-3. 절의 첫 문장은 소재가 아니라 판단을 말한다. `문제는`, `다만`, `반대로`, `결국`, `요컨대`를 논리관계가 있을 때 사용해 일반론-조건-예외-사례를 연결한다.
8-4. 대절의 결론, 혼동하기 쉬운 구분, 반론 뒤에도 남는 판단만 `**굵게**` 표시한다. 굵은 부분만 이어 읽어도 핵심 논지가 복원되어야 하며 문단 전체·감정적 표현·출처 자체는 굵게 하지 않는다.
9. 개인 경험, 반복 경험, 커뮤니티 통념, 편집자 종합을 구분하되 가치중립적 분포 보고를 목표로 삼지 않는다.
10. 수치·연도·금액·점수·사건·정책·변화·다수성 주장·대표 사례·반론에는 각주를 붙인다. 각주는 출처 표식에 그치지 않고 본문에서 덜어 낸 세부 절차·계산·원문의 비유와 은어·작은 반례·후일담·출처의 한계를 싣는 보조 서술층으로 쓴다.
10-1. 본문은 각주 없이도 핵심 논지가 완결되어야 하고, 각주를 읽으면 반드시 새로운 디테일을 얻을 수 있어야 한다. 각주는 출처·세부·트리비아·반론·한계의 다섯 역할로 구분한다. 한 원문에 서로 다른 세부가 있으면 표준 ID 뒤에 `-detail`, `-trivia`, `-counter`, `-limit` 접미사를 붙여 각주를 나눈다.
10-1-1. 각주 내부는 `DB 위치 → 본문에 없는 추가 정보 → 사실 지위 → 필요시 한계` 순서로 쓴다. 각주는 정확한 주장 직후에 호출하고, 문단 끝의 인용 더미를 만들지 않는다. 한 각주는 하나의 중심 곁가지만 다룬다.
10-2. 원문의 농담·과장·비유는 해당 작성자의 표현임을 밝혀야 하며, 개인정보·혐오·명예훼손 위험이 있는 트리비아는 제외한다.
11. 일반 게시물은 G/A-P-ID, 리뷰 게시물은 G/A-RP-ID, 댓글은 G/A-C-ID, 게시물과 핵심 댓글을 함께 쓰는 스레드는 G/A-T-ID 형식으로 표기한다.
12. 댓글은 부모 게시물과 부모의 소분류를 확인한다. 고아 댓글은 쓰지 않는다.
13. 동일 게시물 중복본을 복수 근거로 계산하지 않는다.
14. 제목만 보지 말고 본문을 읽고, 필요한 경우 댓글을 확인한다. 기존 노트의 ID는 검색 시드일 뿐 정답 집합이 아니다.
14-1. `tools/mine_grad_abroad_sources.py`로 기존 ID·메타데이터·장문·고댓글·제목·본문·댓글·반론 검색 경로의 합집합을 만들고, V3의 정독 순서와 포화 중단 기준을 기록한다.
15. source_url, 실명, 연락처, 이메일과 개인 특정 정보를 최종 문서에 쓰지 않는다.
16. 은어는 반복 사용되고 이해에 필요할 때만 보존하며 최초 1회 설명한다.
17. 편견, 조롱, 과장, AI 평가체, 근거 없는 인과관계를 제거한다.
18. 시기 구분은 이전·이후 각각 복수의 독립 근거가 있을 때만 사용한다.
19. 오래된 정보는 과거형과 적용 시기를 명시한다.
20. 다른 소분류 근거는 원 분류와 사용 이유를 근거 장부에 기록한다.
21. 작성 전 V3에서 계승한 방식으로 주제 질문 지도, 검색 로그, 앵커 스레드 카드, 원자적 정보단위 장부, 편집 논제·주장 그래프를 만든다. V4 필드인 `presentation_layer`와 `footnote_role`도 지정한다. 근거는 A급 핵심 앵커, B급 보조, C급 제외로 판정한다.
21-1. A급 앵커마다 본문에 반드시 보존할 절차·수치·행동·결과·반론과 각주에 보존할 비유·은어·작은 반례·후일담·한계를 별도로 적는다.
21-2. 인용 개수 할당량을 채우지 않는다. 한 개의 고정보 스레드가 다수의 저정보 게시물보다 우선한다.
22. 초안 뒤 핵심 논제별로 반대 결과·다른 원인·다른 시기·다른 행위자·측정 오류를 적대적으로 재검색하고, 강한 반론이 나오면 논제를 수정한다.
22-1. 작성 후 tools/validate_grad_abroad_note.py와 QA_CHECKLIST.md를 사용한다. 근거 정밀도·정보단위 보존·적대적 검토의 삼중 감사, 굵은 부분만 읽는 스캔 감사, 각주만 읽는 주석 감사, 이전 버전 대비 블라인드 비교를 완료한다.
23. 불일치는 자동 추정하거나 덮어쓰지 말고 보류 항목으로 보고한다.
24. 사용자가 명시하지 않으면 index.md 갱신, 커밋, push, publish.ps1 실행을 하지 않는다.
25. 완료한 문서는 STATUS.md의 상태와 검사 결과를 갱신한다. 미완료라면 외부 근거 폴더에 HANDOFF.md를 남긴다. `DISCOVERY.md`, `SOURCE_CARDS.md`, `INFORMATION_UNITS.md`, `CLAIM_GRAPH.md`, `QA.md`가 없으면 완료로 판정하지 않는다.

응답 순서:
A. 기존 문서 진단
B. 핵심 명제와 근거 장부 요약
C. 후보 발견·포화·정보단위 보존 요약
D. 새 목차와 주장 그래프
E. 실제 파일 수정
F. 자동 검사·삼중 감사·블라인드 비교 결과
G. 보존된 메타데이터와 링크
H. 남은 불확실성
```

## 짧은 지시 예시

- `GRAD G-014를 표준 규격으로 변환해줘.`
- `ABROAD의 학생비자 문서를 변환하고 QA까지 해줘. 배포는 하지 마.`
- `A-037 작업을 핸드오프 파일에서 이어서 완료해줘.`
- `GRAD/ABROAD 전체에서 완료 문서의 자동 QA만 실행해줘.`
- `시범 문서 4개 중 다음 미완료 문서를 작업해줘.`
- `G-015를 V4로 변환하고 증거 산출물·적대적 검색·스캔·주석 감사까지 완료해줘. 배포는 하지 마.`
